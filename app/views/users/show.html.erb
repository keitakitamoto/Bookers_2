<span class= "error" ><% if @book.errors.any? %>
 <h3><%= @book.errors.count %>errors prohibited this obj from being saved:</h3>
<% @book.errors.full_messages.each do |message| %>
 <li><%= message %></li>
<% end %>
<% end %></span>
<p id="notice"></p>
<div class="container">
 <div class="row">
   <div class="col-md-3">
     <%= render 'users/list',user:@user ,book:@book %>
    </div>
  <div class="offset-md-1 col-md-8">
    <h2>Books</h2>
    <table class="table table-hover table-inverse">
      <thead>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
      </thead>
      <tbody>
       <% @books.each do |book| %>
       <tr>
        <th><%= link_to (attachment_image_tag @user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg",size:'40×40'),user_path(@user) %></th>
        <th><%= link_to book.title,book_path(book) %></th>
        <th><%= book.body %></th>
        <th colspan="3"></th>
       </tr>
      </tbody>
       <% end %>
    </table>
  </div>
 </div>
</div>